# pluginrack CLI

Orchestration CLI for the `plugin-rack` repo. uv-managed.

```
uv sync
uv run pluginrack --help
```

Shape: `pluginrack [global-args] <command> [cmd-args] [-- wrapper-args]`. `--help` at every level.
